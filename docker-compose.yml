version: '3'

services:
  shoppingcartapi:
    image: shoppingcartapi
    environment:
      - ConnectionString=shoppingcart.data
    build:
      context: ./ShoppingCartApi
      dockerfile: Dockerfile
    depends_on:
      - shoppingcart.data
  shoppingcart.data:
    image: redis
